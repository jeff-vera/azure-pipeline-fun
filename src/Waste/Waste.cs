using System;

namespace Waste
{
    public class Waste
    {
        public int Subtract(int x, int y)
        {
            return x - y;
        }

        public double PowerOf(int x, int y)
        {
            return Math.Pow(x, y);
        }
    }
}
